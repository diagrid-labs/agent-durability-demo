version: 1
common:
  appLogDestination: console
  daprdLogDestination: console
  enableAppHealthCheck: false
  logLevel: info
apps:
  - appID: bank-agent-creditor
    appDirPath: ./services/agent
    daprHTTPPort: 3500
    appPort: 8000
    appProtocol: http
    command: ["uv", "run", "uvicorn", "agent_worker.main:app", "--host", "0.0.0.0", "--port", "8000"]
    env:
      STUB_LLM: "true"
      LOG_LEVEL: INFO
